// Update this page (the content is just a fallback if you fail to update the page)

import GitHubWrapped from "@/components/GitHubWrapped";

const Index = () => {
  return <GitHubWrapped />;
};

export default Index;
